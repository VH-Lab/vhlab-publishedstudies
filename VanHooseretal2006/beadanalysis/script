

roicoord = roipts(:,2:3)-repmat([cellpts(1,2),cellpts(1,3)],length(roipts),1);

  cellcoords = cellpts(:,2:3)-repmat([cellpts(1,2),cellpts(1,3)],length(cellpts),1);

  scale=1/((0.750 / 1024)*182.3);
